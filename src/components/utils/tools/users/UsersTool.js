import React from 'react';
import {Form} from "react-bootstrap";

const UsersTool = () => {
    return (
            <Form>
                            <Form.Group>
                <Form.Text aria-placeholder={'Qidirish...'}></Form.Text>
                <Form.Text aria-placeholder={'Qidirish...'}></Form.Text>
            </Form.Group>
            </Form>
    );
};

export default UsersTool;